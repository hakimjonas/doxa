// `id{A: Type}` with an implicit type parameter: call sites elide it and the
// elaborator inserts a fresh metavariable solved by pattern unification.
data Nat: Type {
  zero: Nat;
  succ: Nat -> Nat;
}

fun id{A: Type}(x: A): A = x

val n: Nat = id zero
