name: doxa_monorepo
description: Doxa monorepo root — run `tool/` scripts from here.
publish_to: none
version: 0.8.2
repository: https://github.com/hakimjonas/doxa

environment:
  sdk: ^3.7.0

dependencies:
  doxa:
    path: doxa
  doxa_tooling:
    path: doxa_tooling

dependency_overrides:
  doxa:
    path: doxa
  doxa_tooling:
    path: doxa_tooling
